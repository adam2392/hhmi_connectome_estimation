from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='An exploration of VAE on neural activity, with the decoder as a biophysical dynamical model."',
    author='HHMI Neural Activity',
    license='MIT',
)
